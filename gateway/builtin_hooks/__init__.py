"""Built-in gateway hooks — reserved for always-on system hooks.

Currently empty. System hooks (session start/end, agent step, etc.)
are emitted directly from run.py via gateway/hooks.py emit().
User-configurable hooks live in ~/.hermes/hooks/.
"""
